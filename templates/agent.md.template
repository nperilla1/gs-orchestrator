---
name: <!-- REPLACE: agent-name (lowercase, hyphenated) -->
description: "<!-- REPLACE: One sentence describing the agent's role and expertise -->"
model: <!-- REPLACE: haiku | sonnet | opus -->
tools:
  # Include only the tools this agent needs. Fewer tools = more focused agent.
  # Common tool sets:
  #   Read-only analysis: Read, Grep, Glob, Bash
  #   Code modification:  Read, Edit, Write, Bash, Grep, Glob
  #   Research:           Read, Grep, Glob, Bash, WebFetch, WebSearch
  #   Full access:        Read, Edit, Write, Bash, Grep, Glob, WebFetch, WebSearch
  - Read
  - Grep
  - Glob
  - Bash
---

# <!-- REPLACE: Agent Name -->

<!-- REPLACE: 1-2 paragraphs explaining who this agent is, what domain expertise it has, and how it should approach problems. Write in second person ("You are a..."). -->

## Capabilities

<!-- REPLACE: List the specific things this agent can do. Group by category if there are many. -->

### <!-- REPLACE: Capability Category 1 -->

<!-- REPLACE: Describe what the agent does in this category. Include example commands or patterns. -->

```bash
# <!-- REPLACE: Example command or code pattern -->
```

### <!-- REPLACE: Capability Category 2 -->

<!-- REPLACE: Describe what the agent does in this category. -->

## Protocol

<!-- REPLACE: Define the step-by-step process the agent should follow. Number the steps. -->

1. **<!-- REPLACE: Step name -->** -- <!-- REPLACE: what to do -->
2. **<!-- REPLACE: Step name -->** -- <!-- REPLACE: what to do -->
3. **<!-- REPLACE: Step name -->** -- <!-- REPLACE: what to do -->

## Output Format

<!-- REPLACE: Define the structure of the agent's output. Use a template if the output is structured. -->

```
## <!-- REPLACE: Output section name -->
- **<!-- REPLACE: Field -->**: <!-- REPLACE: description -->
- **<!-- REPLACE: Field -->**: <!-- REPLACE: description -->
```

## GS-Specific Context

<!-- REPLACE: Add any GrantSmiths-specific knowledge this agent needs. -->
<!-- Delete this section if the agent is not GS-specific. -->

## Rules

<!-- REPLACE: List the constraints and rules the agent must follow. -->
- <!-- REPLACE: Rule 1 -->
- <!-- REPLACE: Rule 2 -->
- <!-- REPLACE: Rule 3 -->
